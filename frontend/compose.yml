services:
  api:
    build: ..
    environment:
      HTTP_ADDR: ":8080"
      PYTHON_BIN: python3
      RUNNER_SCRIPT: python/runner.py
    ports:
      - "8080:8080"
  web:
    build: .
    ports:
      - "5173:80"
    depends_on:
      - api
